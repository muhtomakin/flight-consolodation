This is the back end side for flight consodilation project.

# Start Project

-- Create virtual env

`py -m <env>`
`./<env>/Scripts/activate`
`pip install -r requirements.txt`
`cd api`
`py manage.py runserver`
